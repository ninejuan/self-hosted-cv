export function meta() {
    return [
        { title: "Dashboard — Self-Hosted CV" },
    ];
}

export default function AdminDashboard() {
    return (
        <div>
            <p style={{ color: "var(--color-text-muted)" }}>Admin dashboard — coming soon</p>
        </div>
    );
}
